{
  "//": [
    "SHELL-Q40: this file holds no endpoint, no token policy and no bus.",
    "An installed application does not carry service topology — the Kernel",
    "composes it and the SDK materializes it. It used to carry Identity's",
    "endpoint, Context's, the Kernel's and the bus, under a comment saying a",
    "package must not do that; ed38380 closed the gap the comment described.",
    "What is left is the development connection string, so `migrate` runs from",
    "a checkout."
  ],
  "ConnectionStrings": {
    "HotelOS": "Host=localhost;Port=25432;Database=hotelos;Username=hotelos_app_guestops;Password=devguestops"
  },
  "Logging": {
    "LogLevel": {
      "Default": "Information"
    }
  }
}
