{
  "name": "@hotelos-apps/pms-oracle-ui",
  "version": "0.1.0",
  "description": "The Oracle connector's configuration form, hosted by Software Center.",
  "type": "module",
  "private": true,
  "license": "UNLICENSED",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "build": "node ../../scripts/build-module.mjs",
    "test": "npm run build && vitest run"
  },
  "devDependencies": {
    "esbuild": "^0.28.2",
    "typescript": "^5.6.3"
  }
}
